type Query {
  users: [User]
  user(id: ID!): User
}

type Mutation {
  createUser(CreateUserInput: CreateUserInput): User
}

type Subscription {
  userCreated: User
}

type User {
  id: Int
  name: String
  age: Int
}

input CreateUserInput {
  name: String
  age: Int
}
